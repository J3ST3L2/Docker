---
http:
  cache:
    type: redis
    server: redis:6379
database:
  saved-filters:
    - description: "Internal traffic"
      content: >-
        SrcNetRole = internal OR DstNetRole = internal
    - description: "WAN or uplink interfaces"
      content: >-
        InIfBoundary = external OR OutIfBoundary = external
