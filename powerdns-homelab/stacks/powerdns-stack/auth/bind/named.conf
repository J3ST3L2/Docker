options {
  directory "/var/lib/powerdns/zones";
};

zone "tape.local" {
  type secondary;
  primaries { 10.20.60.10; 10.20.60.14; };
  file "tape.local.zone";
};

// Optional reverse zones. Enable only for reverse zones that exist in AD DNS.
// zone "20.10.in-addr.arpa" {
//   type secondary;
//   primaries { 10.20.60.10; 10.20.60.14; };
//   file "20.10.in-addr.arpa.zone";
// };
